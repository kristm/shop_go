package tui

/*
func MoveRight(m *model) {
	m.rowIndex = 0 //reset to first row when changing tabs
	if m.cursor < len(m.sections)-1 {
		m.cursor++
	} else {
		m.cursor = 0
	}
}

func MoveLeft(m *model) {
	m.rowIndex = 0 //reset to first row when changing tabs
	if m.cursor > 0 {
		m.cursor--
	} else {
		m.cursor = len(m.sections) - 1
	}
}

func MoveUp(m *model) {
	if m.rowIndex > 0 {
		m.rowIndex--
	}
}

func MoveDown(m *model) {
	if m.rowIndex < m.tableRows {
		m.rowIndex++
	}
}

func ToggleModal(m *model) {
	m.modal.visible = !m.modal.visible
}*/
